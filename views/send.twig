<!DOCTYPE html>
<html lang="en">
<head>
    {% include './head.twig' %}
</head>
<body>
{% include './nav.twig' %}
<main role="main" class="container">
    <div class="row align-items-center">
        <h1>Here you can send some message from predefined!</h1>
    </div>
    <div class="row align-items-center">
        <form action="/spam" method="post">
            <div class="form-group row">
                <select class="form-select" name="mess">
                    {% for ms in messages %}
                        <option value="{{ loop.index0 }}">{{ ms }}</option>
                    {% endfor %}
                </select>
            </div>
            <div class="form-group row">
                <div class="col-sm-10">
                    <button type="submit" class="btn btn-primary">Send</button>
                </div>
            </div>
        </form>
    </div>
</main>
</body>
</html>